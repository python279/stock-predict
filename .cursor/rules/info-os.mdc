---
description: Info-OS news, market-risk report, and operational conventions
alwaysApply: true
---

# Info-OS 项目规则

## 运行与安全
- 在项目根目录使用 `.venv/bin/python3` 运行 Python。
- `config.yaml` 包含密钥和邮箱凭据，禁止打印、提交或复制其敏感值；配置结构改动须同步 `config.example.yaml` 和 README。
- 外部新闻、行情、LLM、邮件调用必须有超时、重试、日志和降级路径；单个源失败不得中断日报。

## 数据与新闻
- 新闻来源优先使用 RSS、GDELT、Google News 白名单和 NewsAPI；聚合源只保留可信媒体。
- NewsAPI/GDELT 遇 HTTP 429 时停止该源本轮后续请求，不做无效重试；继续使用其他来源。
- 文章需去重、按关键词打 `tech`/`finance`/`consumer`/`policy` 标签，并安全处理缺失字段。
- 市场结论必须标注数据日期和来源；行情或政策数据缺失时写明“数据缺失/未采集”，不得补造。

## 市场与风险分析
- A股重点覆盖科技（芯片、AI、存储）、金融和大消费；美股覆盖科技、金融、消费及风险偏好。
- 科技急跌仅能做脆弱性预警：基于相对宽基表现、回撤、均线、量能、新闻和政策信号；不得称为可确定预测。
- 投资内容是情景分析，不保证收益；杠杆、期权、期货必须标注高风险与失效/止损条件。

## 报告与邮件格式
- 报告固定使用 7 个连续主章节：决策摘要、风险监测、宏观跨资产、A股、美股、港日联动、未来执行清单。
- 同一新闻只在最相关章节完整解释一次，其他章节仅交叉引用；优先输出结论、风险和触发条件。
- 使用 Markdown `###`/`####`、列表和表格；禁止 HTML、LaTex、代码块及跳号章节。
- 表格单元格禁止列表、换行和未转义 `|`；多个条件用 `；` 分隔。邮件渲染前应兼容清理 LaTex 箭头和表格内行内列表。

## 存储、测试与文档
- 新闻缓存、市场/舆情快照和报告均应可序列化；历史窗口与每日文章上限从 `storage` 配置读取。
- 实质代码改动后，至少运行相关离线单元测试、`py_compile` 和配置 YAML 校验；检查新改文件的 linter 诊断。
- 文档应如实反映代码行为：当前内置调度器会在每日 `run_time` 和每小时整点触发；不要宣称只会每日执行。
